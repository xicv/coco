# coco runtime state + local PM store (.coco/store/) — the whole tree is local and never committed
.coco/

# node
node_modules/
dist/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# os / editor
.DS_Store
